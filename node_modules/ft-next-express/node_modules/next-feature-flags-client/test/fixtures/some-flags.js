'use strict';

var today = new Date();
var week = 1000 * 60 * 60 * 24 * 7;
var future = new Date(today.getTime() + week);
var recentPast = new Date(today.getTime() - week);
var distantPast = new Date(today.getTime() - (8 * week));

module.exports = [
	{
		"name": "contextNavigation",
		"description": "on = shows the left-hand context navigation component, off = hides it from the user",
		"safeState": false,
		"expiry": future.toISOString(),
		"state": false
	},
	{
		"name": "video",
		"description": "on = embeds videos instream, off = hides the video and displays text instead",
		"safeState": false,
		"expiry": future.toISOString(),
		"state": true
	},
	{
		"name": "longExpired",
		"description": "on = embeds videos instream, off = hides the video and displays text instead",
		"safeState": false,
		"expiry": distantPast.toISOString(),
		"state": true
	},
	{
		"name": "recentlyExpired",
		"description": "on = embeds videos instream, off = hides the video and displays text instead",
		"safeState": false,
		"expiry": recentPast.toISOString(),
		"state": true
	}
];
